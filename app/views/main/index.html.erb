<% @propositions.each do |prop| %>
  <div class="proposition"
    <h1><%= prop.title %></h1>
    <p><%= prop.description %></p>

    <table>
      <tr>
        <td><%= prop.currency1_id %></td>
        <td><%= prop.currency2_id %></td>
      </tr>
    </table>
  </div>
<% end %>